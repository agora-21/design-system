// const dsComponents = import.meta.glob('@/components/*.vue')

// const exportedDsComponents = {}

// for (const path in dsComponents) {
//   if (dsComponents.hasOwnProperty(path)) {
//     const component = dsComponents[path]
//     exportedDsComponents[component] = component
//   }
// }

// export default exportedDsComponents

import DsButton from '@/components/DsButton.vue'

export const DsButton
